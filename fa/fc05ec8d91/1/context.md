# Session Context

## User Prompts

### Prompt 1

In /Users/jaronswab/go/src/github.com/jrswab/axe, run `make check-diff` and return the full output. If the Makefile doesn't have a `check-diff` target, run `cat Makefile` first to see what targets are available, then run the closest equivalent (like `make lint` or `make test`).

