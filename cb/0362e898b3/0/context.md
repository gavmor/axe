# Session Context

## User Prompts

### Prompt 1

In the axe project at /Users/jaronswab/go/src/github.com/jrswab/axe, commit and push the updated release skill.

Run these commands in order:

1. `cd /Users/jaronswab/go/src/github.com/jrswab/axe && git add -A`
2. `git diff --cached --stat` (show what's staged)
3. `git commit -m "docs: expand release skill with version bump, README, and GHCR steps"`
4. `git push origin master`

Return the full output of each command.

