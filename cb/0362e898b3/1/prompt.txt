In the axe project at /Users/jaronswab/go/src/github.com/jrswab/axe, fix the nested fenced code block issue in `.opencode/skills/release/SKILL.md`.

The problem is in step 12 where there's a template showing what the release notes should look like. The outer code fence and inner code fence both use triple backticks, which breaks CommonMark parsing.

Find the section that looks like this (around lines 77-85):
```
      ```
      ## Docker

      ```bash
      docker pull ghcr.io/jrswab/axe:X.Y.Z
      ```

      See the [GHCR package page](https://github.com/jrswab/axe/pkgs/container/axe) for all available tags.
      ```
```

Replace the OUTER fences (the first and last triple backticks of that block) with four backticks (``````) so the nesting is valid CommonMark:

```
      ````
      ## Docker

      ```bash
      docker pull ghcr.io/jrswab/axe:X.Y.Z
      ```

      See the [GHCR package page](https://github.com/jrswab/axe/pkgs/container/axe) for all available tags.
      ````
```

After fixing:
1. Apply the exact same fix to `docs/skills/release/SKILL.md`
2. Run `diff .opencode/skills/release/SKILL.md docs/skills/release/SKILL.md` to confirm they still match
3. Read back lines 74-90 of the file to verify the fix

Return the diff output and the relevant lines.