# Session Context

## User Prompts

### Prompt 1

In the axe project at /Users/jaronswab/go/src/github.com/jrswab/axe, update README.md to document the three new features in v1.5.0. Here are the EXACT changes to make:

---

## 1. Features list (around lines 19-33)

Add these three bullet points to the existing features list. Insert them in logical positions:

- After the "Built-in tools" bullet (line 30), add:
  ```
  - **Output allowlist** — restrict `url_fetch` and `web_search` to specific hostnames; private/reserved IPs are always blocked...

