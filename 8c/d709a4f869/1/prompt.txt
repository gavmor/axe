You are working in /Users/jaronswab/go/src/github.com/jrswab/axe on branch ISS-24/allow-list-connections.

## Task
The `TestExecuteCallAgent_AllowedHostsInheritance` test in `internal/tool/tool_test.go` doesn't actually verify the nil-vs-empty distinction. It only checks that the sub-agent ran successfully, which would pass even with the old `len() == 0` logic.

## Problem
The test needs to verify that:
1. When `cfg.AllowedHosts` is nil → the parent's `AllowedHosts` is inherited into the sub-agent's `ExecContext`
2. When `cfg.AllowedHosts` is `[]string{}` → the empty slice is used (NOT the parent's list)

## Approach
The simplest way to test this is to NOT test through `ExecuteCallAgent` (which requires a full provider, TOML files, etc.). Instead, test the logic directly. The logic is just 3 lines:

```go
effectiveAllowedHosts := cfg.AllowedHosts
if effectiveAllowedHosts == nil {
    effectiveAllowedHosts = opts.AllowedHosts
}
```

Replace the existing `TestExecuteCallAgent_AllowedHostsInheritance` test with a simpler, more direct test:

```go
func TestEffectiveAllowedHosts(t *testing.T) {
	tests := []struct {
		name       string
		subAgent   []string // cfg.AllowedHosts (nil or empty or populated)
		parent     []string // opts.AllowedHosts
		wantLen    int
		wantNil    bool
	}{
		{
			name:     "nil sub-agent inherits parent list",
			subAgent: nil,
			parent:   []string{"api.example.com"},
			wantLen:  1,
		},
		{
			name:     "empty sub-agent clears parent list",
			subAgent: []string{},
			parent:   []string{"api.example.com"},
			wantLen:  0,
		},
		{
			name:     "populated sub-agent uses own list",
			subAgent: []string{"docs.example.com"},
			parent:   []string{"api.example.com"},
			wantLen:  1,
		},
		{
			name:    "nil sub-agent with nil parent stays nil",
			subAgent: nil,
			parent:   nil,
			wantNil:  true,
		},
	}

	for _, tc := range tests {
		t.Run(tc.name, func(t *testing.T) {
			// Replicate the logic from ExecuteCallAgent
			effective := tc.subAgent
			if effective == nil {
				effective = tc.parent
			}

			if tc.wantNil {
				if effective != nil {
					t.Errorf("expected nil, got %v", effective)
				}
				return
			}

			if len(effective) != tc.wantLen {
				t.Errorf("expected len %d, got %d (%v)", tc.wantLen, len(effective), effective)
			}
		})
	}
}
```

Wait — this tests the logic in isolation but doesn't prove the production code uses this logic. Let me think of a better approach.

Actually, the best approach: since the logic is inside `ExecuteCallAgent` and is hard to test through that function, and the logic is trivially correct (3 lines), the most valuable test is one that proves the TOML decoding distinguishes nil from empty. This is already tested in `internal/agent/agent_test.go` (`TestAllowedHosts_TOMLParsing`). Let me check if that test covers the nil-vs-empty distinction.

Read `internal/agent/agent_test.go` and find the `TestAllowedHosts_TOMLParsing` test. Check if it has:
- A case where `allowed_hosts` is omitted → `cfg.AllowedHosts` is `nil`
- A case where `allowed_hosts = []` → `cfg.AllowedHosts` is `[]string{}` (non-nil, empty)

If those cases exist and pass, then the TOML layer correctly distinguishes nil from empty, and the logic in `tool.go` (which checks `== nil`) will work correctly.

If the existing test already covers this, then just replace the weak `TestExecuteCallAgent_AllowedHostsInheritance` with the simpler `TestEffectiveAllowedHosts` above (which tests the logic directly) and remove the complex mock-based test.

## Steps

1. Read `internal/agent/agent_test.go` to check the existing TOML parsing tests
2. Read `internal/tool/tool_test.go` to find the current `TestExecuteCallAgent_AllowedHostsInheritance` test
3. Replace it with the simpler `TestEffectiveAllowedHosts` test above
4. Run `go test -run TestEffectiveAllowedHosts -v ./internal/tool/`
5. Run `go test ./internal/tool/`

Return all output.