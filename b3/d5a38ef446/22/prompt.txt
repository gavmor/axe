You are working in the Go project at /Users/jaronswab/go/src/github.com/jrswab/axe.

Your task: In the file `docs/plans/000_url_fetch_timeout_and_html_stripping_milestones.md`, change line 75 from:
```
- [ ] 001: Add 15-second per-request HTTP timeout to `url_fetch` tool
```
to:
```
- [x] 001: Add 15-second per-request HTTP timeout to `url_fetch` tool
```

Only change that one line. Do not modify anything else.

Then stage all changes and commit with the message:
```
feat(url_fetch): add 15-second per-request HTTP timeout

Add context.WithTimeout wrapping the request context so each HTTP
request is governed by a 15-second deadline independent of the parent
agent-runner context. The shorter of the two deadlines wins
automatically via nested Go contexts.

Closes milestone 001 of issue #17.
```

Return confirmation of the commit including the short SHA.