# Session Context

## User Prompts

### Prompt 1

You are working in /Users/jaronswab/go/src/github.com/jrswab/axe. This is a Go project.

Your task is to write ALL the new tests for HTML stripping in `internal/tool/url_fetch_test.go` AND add a minimal stub function in `internal/tool/url_fetch.go` so the tests compile but FAIL (red phase of TDD).

## IMPORTANT: The existing test file has 400 lines. You must APPEND to it, not replace it. Read the file first to understand its structure.

## Current state of url_fetch.go (133 lines):
- Package ...

