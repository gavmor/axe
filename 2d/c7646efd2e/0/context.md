# Session Context

## User Prompts

### Prompt 1

Run `go test ./internal/agent/... -v` in the directory /Users/jaronswab/go/src/github.com/jrswab/axe and return only the failed test results. If all tests pass, say "ALL TESTS PASS" and include the final summary line.

